import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Disfrutando del proceso FIT',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
